syntax="proto3";

// Defines the import path that should be used to import the generated package,
// and the package name.
option go_package = "gitlab.mapcard.pro/external-map-team/api-proto/charge_types";

package charge_types;

import "api_types/api_types.proto";
import "block_types/block_types.proto";

//
// Charge method issues
//
message ChargeRequest {
  BlockHandler block = 1;
} 

message ChargeHandler {
  string id = 1;
}
